#include <iostream>

using namespace std;

float oceny[5]; float suma=0, srednia;

int main()
{
    int liczby[100000];
    liczby[0]=100;
    cout<<"Liczby: "<<endl;
    cout<<liczby[1];
    for (int i=0; i<5; i++)
    {
        cout<<"cyfra"<<endl;
        cin>>oceny[i];
    }
}
